language: C

install: make

services:
  - docker

before_install:
  - docker logout
  - docker login -u $DOCKER_USERNAME --password-stdin $DOCKER_PASSWORD

stages:
  - build
  - name: push
    if: branch = master

jobs:
  include:
    - stage: build
      script: make build
    - stage: push
      script: make push